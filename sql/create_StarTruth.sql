create table if not exists StarTruth (
       id BIGINT,
       raICRS DOUBLE,
       decICRS DOUBLE,
       u_mag FLOAT,
       g_mag FLOAT,
       r_mag FLOAT,
       i_mag FLOAT,
       z_mag FLOAT,
       y_mag FLOAT,
       primary key (id)
       )
